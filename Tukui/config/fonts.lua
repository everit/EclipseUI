local T, C, L = unpack(select(2, ...)) -- Import: T - functions, constants, variables; C - config; L - locales

local template = {
	font = C["media"].font,
	size = 12,
	style = nil,
	shadow = false,
	alignment = { "CENTER", 0, 0 },
}

C["fonts"] = {
	
	aFont = variable or template[1],
	aSize = variable or template[2],
	aStyle = variable or template[3],
	aShadow = variable or template[4],
	aAlignment = variable or template[5],
	
	
	
	
	
	
	
	
	
	
	
	
	
	
}